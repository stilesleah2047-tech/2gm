{
  "name": "g2m-operations",
  "version": "1.0.0",
  "private": true,
  "description": "Field operations and product onboarding for G2M \u2014 Nairobi distribution",
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix web install && node scripts/after-setup.mjs",
    "api": "npm --prefix server run dev",
    "web": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start",
    "verify": "npm --prefix server run verify",
    "mongo": "docker run -d --name g2m-mongo -p 27017:27017 -v g2m-data:/data/db mongo:7"
  }
}
